// write an animated drawing program that moves the blue square fowllowing a square path over the background green circle
# include <graphics.h>

const int WIDTH = 600;
const int HEIGHT = 600;
const int WAIT_TIME = 25;
const int SQUARE_SIZE = 60;
const int MOVE_DISTANCE = 8;

// set the background contents, which is then always displayed and left unmodified
void draw_background() {
    background();
    setColour(green);

    // draw circle
    int x_coords[5];
}

